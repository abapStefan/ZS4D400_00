projection implementation in class ZBP_40_C_FLIGHT unique;
strict ( 2 );
extensible;
use draft;
use side effects;
define behavior for Z40_C_FLIGHT alias FLIGHT
extensible
use etag
{
//  use create;
  use update;
//  use delete;

  field (readonly)   PlaneTypeID;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

}